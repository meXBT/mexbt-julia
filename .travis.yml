language: julia
os:
  - linux
  - osx
julia:
  - release
  - nightly
notifications:
  email: true
script:
 - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
 - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("MexbtClient"); Pkg.test("MexbtClient"; coverage=true)'
